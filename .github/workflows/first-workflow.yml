name: First Workflow

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
       - name: Step 1
         run: echo "Step 1 is completed!"
       - name: Step 2
         run: echo "Step 2 is completed!"         

  job2:
    needs: job1
    runs-on: ubuntu-latest    
    steps:
      - name: Cowsays
        uses: mscoutermarsh/cowsays-action@master 
        run: echo "Step 2 completed!" 
        with:
          text: 'Ready for prod--ship it!'
          color: 'magenta'  