name: First Workflow

on:
    workflow_dispatch:

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
              - name: Run a script
                run: echo "Step 1 complete"
              - name: Run a script
                run: echo "Step 2 complete"